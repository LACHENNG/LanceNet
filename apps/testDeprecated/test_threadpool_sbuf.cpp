#include <iostream>
#include "threadpool.h"

//  use pstack and gdb tookkit to analysis `dead lock`

int main(){
    // int n_threads = 8,  buf_size = 1000;
    // ThreadPool<int> pool(n_threads, buf_size);
    // // ThreadPool<int> pool()


    // auto start = std::chrono::steady_clock::now();
   
    // for(int i = 0; i < 1000; i++){
    //     pool.add_task(i);
    // }
    

    // auto end = std::chrono::steady_clock::now();
    // auto cost_time = std::chrono::duration_cast<std::chrono::duration<double>>(end - start);
    // std::cout << "time : " << cost_time.count() << "s " << std::endl; 


    // exit(0); // the whole process exits
    return 0;    
}